Markov Madness
==============

A command line tool that uses Markov Chains to generate an arbitrary number of
sentences from some number of training corpora.

When fully implemented, usage should look like this:

markov.py -n 3 source1.txt source2.txt
